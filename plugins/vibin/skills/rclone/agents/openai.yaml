interface:
  display_name: "Rclone"
  short_description: "Move, sync, mount, encrypt, and inspect files across cloud..."
  default_prompt: "Use $rclone to move, sync, mount, encrypt, and inspect files across cloud."
