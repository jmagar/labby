# Custom labels for self-hosted runners so actionlint's runner-label check
# recognizes them. tootie-lab-linux carries linux-lab, and agent-os-lab carries
# windows-lab.
self-hosted-runner:
  labels:
    - linux-lab
    - windows-lab
